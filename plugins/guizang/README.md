# Guizang

This plugin indexes public Claude Code and Codex skills published by Guizang.

The included skills are vendored from their source repositories so the marketplace can expose a single Codex-compatible local plugin containing multiple `skills/*/SKILL.md` entries.

See `SOURCES.md` for source repositories and pinned commits.
